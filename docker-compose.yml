services:
  web:
    build:
      context: .
      args:
        # Serve at domain root inside the container.
        VITE_BASE_PATH: /
        # Optional: inject API URL at build time.
        # VITE_API_BASE_URL: http://localhost:5000
    ports:
      - "8080:80"
