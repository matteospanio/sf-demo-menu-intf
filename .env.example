# Copy to .env and adjust as needed.
# Vite exposes only variables prefixed with VITE_.

# Base URL of the SoundFood API (no trailing slash preferred)
VITE_API_BASE_URL=http://localhost:5000

# Optional (production build only): base path where the app is hosted
# Examples: VITE_BASE_PATH=/ (Docker) or VITE_BASE_PATH=/sf-demo-menu-intf/ (GitHub Pages)
# VITE_BASE_PATH=/
